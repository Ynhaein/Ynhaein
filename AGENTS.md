# AGENTS.md

This is a **GitHub profile README** repository, not a code project. The `README.md` renders on the profile page at github.com/Ynhaein.

## Structure

- `README.md` — profile page (badges, links, tech stack)
- `assets/` — images used in the README
- `.github/workflows/pacman.yml` — generates a pacman contribution graph SVG on schedule/push; output lands on `pacman-output` branch

## If editing README.md

- Preserve the HTML/badge structure as-is.
- Badges use `img.shields.io` and `readme-typing-svg.demolab.com` — do not alter the URL patterns.
- The pacman contribution graph section is currently commented out (lines 82–94).

## No build/test/lint

There is no application code, no dependencies, no tests, no build step.
